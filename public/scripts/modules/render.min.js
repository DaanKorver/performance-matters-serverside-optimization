function render(e,r){searchResults.innerHTML="",0===e.length&&(searchResults.innerText=`No results found for '${r}' 😢`),e.forEach(e=>{searchResults.insertAdjacentHTML("beforeend",`

      <li>
      <a href="/book/${e.book_id}">
        <img src="https://raw.githubusercontent.com/JustinLung/the-web-is-for-everyone-scrollbook/${e.book_cover}" />
        <p>${e.name}</p>
      </a>
      </li>
    `)})}export{render};